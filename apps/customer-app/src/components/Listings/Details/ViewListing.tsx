import ViewAsCustomer from "@/components/VehicleBooking/VehicleSummary/ViewAsCustomer";


type Props = { id: string };

export default function ViewListing({ id }: Props) {
  return <ViewAsCustomer vehicle={null} />;
}
